# half-blood-code

- https://kintsugi-programmer.github.io/half-blood-code/

![alt text](docs/assets/images/image.png)

```shell
# mkdocs serve
# mkdocs build
# Deploy documentation using:
mkdocs gh-deploy --force
# Then commit and sync again if needed:
git add .
git commit -m "deploy: update GitHub Pages"
git push
```