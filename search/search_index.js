var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"half-blood-code","text":"<ul> <li>axum-rust-nutshell</li> </ul> <pre><code># mkdocs serve\n# mkdocs build\n# Deploy documentation using:\nmkdocs gh-deploy --force\n# Then commit and sync again if needed:\ngit add .\ngit commit -m \"deploy: update GitHub Pages\"\ngit push\n</code></pre>"},{"location":"axum-rust-nutshell.html","title":"axum-rust-nutshell","text":""},{"location":"axum-rust-nutshell.html#introduction-to-axum","title":"Introduction to Axum","text":"<p>Axum is a web framework in Rust specifically designed for creating web servers with a focus on ergonomics and modularity. </p> <ul> <li>Prerequisites: Axum installed and a new Rust project initialized.</li> </ul>"},{"location":"axum-rust-nutshell.html#setting-up-the-router","title":"Setting Up the Router","text":"<p>The router is the core of the Axum web server, mapping URL paths and HTTP methods to specific handler functions.</p>"},{"location":"axum-rust-nutshell.html#the-create_app-function","title":"The <code>create_app</code> Function","text":"<p>The <code>create_app</code> function is responsible for setting up and returning the router.</p> <ul> <li>It returns a <code>Router</code> type, which is specifically the <code>Router</code> struct in Axum.</li> <li>You initialize it by calling <code>Router::new()</code>.</li> </ul> <pre><code>fn create_app() -&gt; Router {\n    Router::new()\n        .route(\"/health\", get(health_check))\n}\n</code></pre>"},{"location":"axum-rust-nutshell.html#chaining-route-calls","title":"Chaining Route Calls","text":"<p>You chain <code>route</code> calls to define the API endpoints. Each route requires:</p> <ol> <li>A URL Path (e.g., <code>/health</code>)</li> <li>An HTTP Method (e.g., <code>get</code>, <code>post</code>, <code>patch</code>, <code>delete</code>)</li> <li>A Handler Function (e.g., <code>health_check</code>) that processes the request. Note: The handler function is passed directly into the method function, not executed.</li> </ol>"},{"location":"axum-rust-nutshell.html#setting-up-the-async-runtime-and-tcp-listener","title":"Setting Up the Async Runtime and TCP Listener","text":"<p>To run the Axum web server, you need an asynchronous runtime and a TCP Listener.</p>"},{"location":"axum-rust-nutshell.html#the-tokio-async-runtime","title":"The Tokio Async Runtime","text":"<p>Tokio is the most popular async runtime in the Rust ecosystem. Because you need to await the creation of a TCP listener, the main function must be marked as <code>async</code>. * The <code>#[tokio::main]</code> macro: Transforms the <code>async fn main</code> into a regular main function and sets up all the necessary Tokio runtime components behind the scenes so manual setup is not required. It allows the use of <code>async</code>/<code>await</code> everywhere.</p>"},{"location":"axum-rust-nutshell.html#binding-the-tcp-listener-and-serving-the-app","title":"Binding the TCP Listener and Serving the App","text":"<ol> <li>Bind the Listener: Bind a <code>TcpListener</code> on port 3000 across all interfaces (<code>0.0.0.0</code>).</li> <li>Serve the Application: Use <code>axum::serve</code>, which connects the TCP listener to the app (the router) and handles requests until the server is shut down.</li> </ol> <pre><code>#[tokio::main]\nasync fn main() {\n    // Bind the TCP listener\n    let listener = tokio::net::TcpListener::bind(\"0.0.0.0:3000\")\n        .await\n        .expect(\"failed to bind TCP listener\");\n\n    println!(\"server running on localhost:3000\");\n\n    // Initialize the router\n    let app = create_app();\n\n    // Start the Axum web server\n    axum::serve(listener, app)\n        .await\n        .expect(\"failed to start server\");\n}\n</code></pre>"},{"location":"axum-rust-nutshell.html#centralized-error-handling","title":"Centralized Error Handling","text":"<p>Centralizing API errors into a single enum keeps handlers clean and makes error handling consistent across the application. Each variant of the enum represents a specific class of error exposed from handlers.</p>"},{"location":"axum-rust-nutshell.html#defining-the-apierror-enum","title":"Defining the <code>ApiError</code> Enum","text":"<p>An enum is a custom type that can be one of several possible values (called variants). </p> <ul> <li><code>#[derive(Debug)]</code> trait: Allows the errors to be printed for debugging purposes.</li> </ul> <pre><code>#[derive(Debug)]\nenum ApiError {\n    NotFound,\n    InvalidInput { data: String },\n    InternalError,\n}\n</code></pre> <p>Variants:</p> <ul> <li><code>NotFound</code>: Used when a resource does not exist (HTTP 404 error).</li> <li><code>InvalidInput</code>: A special variant representing a Bad Request (HTTP 400). It carries data with it\u2014specifically, a string containing an error message.</li> <li><code>InternalError</code>: Used for an internal server error (HTTP 500).</li> </ul>"},{"location":"axum-rust-nutshell.html#implementing-the-intoresponse-trait","title":"Implementing the <code>IntoResponse</code> Trait","text":"<p>Axum uses the <code>IntoResponse</code> trait to turn return values into actual HTTP responses. By implementing this trait for <code>ApiError</code>, any handler that returns <code>Result&lt;T, ApiError&gt;</code> automatically knows how to transform the error into a proper HTTP response.</p> <ul> <li>A trait is similar to an interface in other languages. In Rust, you can implement traits and methods for enums.</li> </ul> <pre><code>impl IntoResponse for ApiError {\n    fn into_response(self) -&gt; Response {\n        let (status, error_message) = match self {\n            ApiError::NotFound =&gt; (StatusCode::NOT_FOUND, \"data not found\".to_string()),\n            ApiError::InvalidInput { data: msg } =&gt; (StatusCode::BAD_REQUEST, msg),\n            ApiError::InternalError =&gt; (StatusCode::INTERNAL_SERVER_ERROR, \"internal server error\".to_string()),\n        };\n\n        let body = axum::Json(serde_json::json!({\n            \"error\": error_message\n        }));\n\n        (status, body).into_response()\n    }\n}\n</code></pre> <p>Key Concepts in <code>IntoResponse</code> Implementation:</p> <ul> <li><code>match self</code>: Similar to a simple switch statement, but <code>self</code> refers to the specific instance of the enum being called. It differentiates return values for each individual variant.</li> <li>Pattern Destructuring: Seen in the <code>InvalidInput { data: msg }</code> arm. This syntax pulls out the string value stored inside the variant and binds it to the variable <code>msg</code> so it can be used as a return value.</li> <li>Axum JSON Extractor: Wrapping the body in <code>axum::Json</code> automatically sets the <code>Content-Type: application/json</code> header and serializes the web data into JSON format.</li> </ul>"},{"location":"axum-rust-nutshell.html#application-endpoints","title":"Application Endpoints","text":""},{"location":"axum-rust-nutshell.html#1-health-check-endpoint-health","title":"1. Health Check Endpoint (<code>/health</code>)","text":"<p>A tiny endpoint to verify if the server is up and running.</p> <ul> <li>Return Type: <code>impl IntoResponse</code>. This means anything that can be turned into a response will be returned.</li> <li>Response Body: Returns JSON utilizing the <code>json!</code> macro containing a status (<code>\"ok\"</code>) and a message (<code>\"server is running\"</code>).</li> </ul>"},{"location":"axum-rust-nutshell.html#2-list-users-endpoint-users","title":"2. List Users Endpoint (<code>/users</code>)","text":"<p>Designed to demonstrate error handling by simulating a failure.</p> <ul> <li>Return Type: <code>Result&lt;Json&lt;Value&gt;, ApiError&gt;</code>. <ul> <li><code>Result</code> is a standard Rust enum for operations that can either succeed (<code>Ok</code> containing the success value) or fail (<code>Err</code> containing the error value).</li> </ul> </li> <li>Logic: Immediately returns <code>Err(ApiError::InternalError)</code>. Axum catches this and seamlessly uses the <code>IntoResponse</code> implementation to generate the standard JSON response.</li> </ul>"},{"location":"axum-rust-nutshell.html#3-get-user-endpoint-usersid","title":"3. Get User Endpoint (<code>/users/:id</code>)","text":"<p>Demonstrates extracting dynamic path parameters from the URL.</p> <ul> <li>Dynamic Path Parameter (<code>:id</code>): Axum automatically captures the value in that part of the URL and makes it available to the handler via the Path extractor.</li> </ul> <pre><code>async fn get_user(Path(id): Path&lt;u32&gt;) -&gt; Result&lt;Json&lt;Value&gt;, ApiError&gt; {\n    if id &gt; 100 {\n        return Err(ApiError::NotFound);\n    }\n\n    Ok(Json(serde_json::json!({\n        \"id\": id,\n        \"name\": \"user\"\n    })))\n}\n</code></pre> <p>Path Extraction Concepts:</p> <ul> <li><code>Path&lt;u32&gt;</code>: The path extractor pulls the ID from the URL and parses it into a <code>u32</code> integer.</li> <li>Destructuring Syntax (<code>Path(id)</code>): This specific syntax extracts the inner ID directly for use in the function.</li> <li>Edge Case: If the value cannot be parsed (e.g., passing letters instead of a number), Axum automatically rejects the request with a 400 Bad Request error.</li> </ul>"},{"location":"axum-rust-nutshell.html#testing-the-web-server","title":"Testing the Web Server","text":"<p>Testing ensures the router and error handling work properly by spinning up the router in memory and sending synthetic requests through it.</p>"},{"location":"axum-rust-nutshell.html#test-setup","title":"Test Setup","text":"<ul> <li><code>mod tests</code> module: Define a module for testing.</li> <li><code>#[cfg(test)]</code> macro: Ensures the tests are only compiled when running the test suite.</li> <li><code>use super::*;</code>: Brings all items from the parent module (the main code) into the scope of the test module.</li> <li><code>#[tokio::test]</code> macro: Used instead of a standard test macro because the tests utilize the Tokio async runtime.</li> </ul>"},{"location":"axum-rust-nutshell.html#testing-the-health-check-endpoint","title":"Testing the Health Check Endpoint","text":"<ol> <li>Construct a Request: Use <code>Request::builder()</code> to construct a GET request to <code>/health</code>. Use <code>Body::empty()</code> since a GET request has no body. The HTTP method defaults to GET automatically.</li> <li>Send Request In-Memory: Use the <code>oneshot</code> function on the app router: <code>app.oneshot(request).await.unwrap()</code>.<ul> <li>Note on <code>oneshot</code>: This function comes from the <code>tower</code> service extension trait. It allows sending a single request directly to the app's router and getting a response back entirely in memory. It is much more reliable and faster for testing than starting a real web server and sending requests over a network, as there is zero network overhead.</li> </ul> </li> <li>Assertions:<ul> <li>Status Code: Assert that the response status equals <code>StatusCode::OK</code> (200).</li> <li>Parse Body: Read the full response body into memory using <code>response.into_body().collect().await.unwrap()</code>. Convert it to a bytes object using <code>.to_bytes()</code>.</li> <li>Validate JSON: Parse the bytes into a structured JSON value using <code>serde_json::from_slice(&amp;body).unwrap()</code>. A reference is used because <code>from_slice</code> does not want to own the value. Verify that the JSON contains <code>status: \"ok\"</code> and <code>message: \"server is running\"</code>.</li> </ul> </li> </ol>"},{"location":"axum-rust-nutshell.html#testing-the-apierror-intoresponse-implementation","title":"Testing the <code>ApiError</code> <code>IntoResponse</code> Implementation","text":"<p>To verify that custom errors map to the correct HTTP status codes, use a standard vector of test cases.</p> <ol> <li>Define a vector where each individual element is a tuple containing:<ul> <li>The specific <code>ApiError</code> variant.</li> <li>The expected HTTP <code>StatusCode</code>.</li> </ul> </li> <li>Iterate over the vector using a <code>for</code> loop.</li> <li>For each case, convert the error into a response manually by calling <code>.into_response()</code>.</li> <li>Assert that the resulting <code>response.status()</code> equals the expected status code.</li> <li>Run tests using the <code>cargo test</code> command.</li> </ol>"},{"location":"axum-rust-nutshell.html#deployment-and-hosting-note-savala","title":"Deployment and Hosting Note (Savala)","text":"<p>Note: This information pertains to a sponsored platform discussed for deploying the web server.</p> <p>Savala is an all-in-one Platform as a Service (PaaS) for deploying apps, databases, and static sites without dealing with complex infrastructure.</p> <ul> <li>Features &amp; Limits: No artificial limits, no restrictions on parallel builds, and no caps on team members. You only pay for what you use.</li> <li>Infrastructure: Runs on Google Kubernetes Engine across 25 regions, utilizing Cloudflare's global network for speed and reliability.</li> <li>Support: Provides support from real human developers who understand technical problems.</li> <li>Developer Experience: Seamless workflow allowing you to push to Git, receive automatic builds, enjoy instant previews, and benefit from one-click templates. Scales with the user from side projects to large applications.</li> <li>Promotion: Offers new users $50 in free credits to get started.</li> </ul>"}]}